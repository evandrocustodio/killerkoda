3. Configure o nome do usuário (author) e o email.

```plain
    git config --global user.name "John Doe"`
    git config --global user.email "john@doe.org"
```
3.2 Verifique as configurações do git:

`git config -l`{{exec}}
